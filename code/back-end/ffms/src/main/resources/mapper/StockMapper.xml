<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.csu.ffms.persistence.StockMapper">

    <select id="getStockByStockCode"  resultType="Stock">
        select
            stockcode as code,
            userid as userid,
            stockname as name,
            stockquantity as quantity,
            cost
        from stock
        where stockcode=#{code} and userid=#{userid}
    </select>

    <select id="getStockByUserId" parameterType="string" resultType="Stock">
        select
            stockcode as code,
            userid as userid,
            stockname as name,
            stockquantity as quantity,
            cost
        from stock
        where userid=#{value}
    </select>

    <insert id="insertStock" parameterType="Stock">
        insert into stock(stockcode,userid,stockname,stockquantity,cost)
        values(#{code},#{userid},#{name},#{quantity},#{cost})

    </insert>

    <delete id="deleteStock" parameterType="Stock">
        delete from stock
        where stockcode=#{code} and userid=#{userid}
    </delete>

    <update id="updateStock" parameterType="Stock">
        update stock set
            stockname=#{name},
            stockquantity=#{quantity},
            cost=#{cost}
        where stockcode=#{code} and userid=#{userid}
    </update>

</mapper>